# Copia este archivo a secrets.properties (NO subir secrets.properties a Git).
# Obtén la clave en: https://console.cloud.google.com/google/maps-apis/credentials
#
# Restringe la clave por:
# - Android: package com.example.myworksapp + SHA-1 del keystore debug/release
# - iOS: bundle ID del proyecto

GOOGLE_MAPS_API_KEY=
